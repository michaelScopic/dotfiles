# === Void Linux aliases for package management ===
## You are always welcome to add/edit your own aliases :)
## PS: I don't use Void, so I don't know much about xbps, so a PR is accepted 
alias reconfigure-pkgs="sudo xbps-reconfigure -afv"
alias up="sudo xpbs-install -Su"
alias pkg-install="sudo xbps-install -S"
